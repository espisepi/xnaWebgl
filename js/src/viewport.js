/**
 * @param {double} x
 * @param {double} y
 * @param {double} width
 * @param {double} height
*/
function Viewport(x, y, width, height) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
}